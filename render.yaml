services:
  - type: web
    name: staging-api
    runtime: docker
    region: ohio
    plan: 'free'
    envVars:
      - key: DEBUG
        value: true
      - key: ENVIRONMENT
        value: staging
